[train]
depth = 8
learning_rate = 0.05
l2_leaf_reg = 1
hypoptim = False
save_dir = ./runs/train1/

[data]
featured_path = data/Reviews_featurized.csv
target_column = Score

[inference]
model_path = ./runs/train1/model.cbm

[database]
host = ${DB_HOST}
port = ${DB_PORT}
dbname = ${DB_NAME}
user = ${DB_USER}
password = ${DB_PASSWORD}
