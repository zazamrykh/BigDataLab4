# This file is only used for initial setup and will be removed in production
# In production, all secrets are stored in Vault

# Database configuration for initial Vault setup
DB_HOST=postgres
DB_PORT=5432
DB_NAME=reviewdb
DB_USER=postgres
DB_PASSWORD=postgres

# Vault configuration
VAULT_ADDR=http://vault:8200
